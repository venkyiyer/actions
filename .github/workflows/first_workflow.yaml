name: first_workflow

on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print hello
        run: echo 'Hello World!'
      - name: print goodbye
        run: echo 'Good bye World'

